<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.serea.mytest20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BloodpressureActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".GpsActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".HeartrateActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".PillowactivityActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".Pillow2Activity"
            android:label="@string/app_name"></activity>
        <activity android:name=".SleepqualityActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".SosActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".WelcomeActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".Page1Activity"
            android:label="@string/app_name"></activity>
    </application>

</manifest>